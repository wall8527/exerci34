/*1) Programa una función que cuente el número
de caracteres de una cadena de texto, pe.
miFuncion("Hola Mundo") devolverá 10. ok */
import {txtin,validatein} from "/js/txtin.js";


    function txtLength() {
    if (txtin === ""){


    }else if(validatein >= 0 || validatein <= 0){

    }else{
      txtin !== ""
      console.log(`El número de caracteres de la cadena de texto ${txtin} es: ${txtin.length}`) ;
      return alert(`El número de caracteres de la cadena de texto ${txtin} es: ${txtin.length}`);
    }

    }
txtLength();
