/*3) Programa una función que dada una String te devuelva un Array de textos
separados por cierto caracter, pe. miFuncion('hola que tal', ' ')
devolverá ['hola', 'que', 'tal'].ok*/
import {txtin,validatein} from "/js/txtin.js";


    function txtSeparate() {
    if (txtin === ""){


    }else if(validatein >= 0 || validatein <= 0){


    }else{
      txtin !== ""
      console.log(txtin.split(" "));
      return alert(`el array de textos, de la cadena de texto ${txtin} es: ${txtin.split(" ")}`)

    }

    }
txtSeparate();
