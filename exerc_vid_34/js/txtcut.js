/*2) Programa una función que te devuelva el texto recortado
según el número de caracteres indicados,
pe. miFuncion("Hola Mundo", 4) devolverá "Hola".*/
import {txtin,validatein} from "/js/txtin.js";


    function txtCut() {
    if (txtin === ""){


    }else if(validatein >= 0 || validatein <= 0){


    }else{
      txtin !== ""
      console.log(txtin.substring(0,4));
      return alert(`el resultado de recortar la cadena de texto ${txtin} es: ${txtin.substring(0,4)}`)

    }

    }
txtCut();
